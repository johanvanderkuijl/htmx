# Flask + HTMX sample app

## Backend
A standard flask application with an in memory database of Pydantic objects.

## Frontend
The frontend consists of rendered jinja2 templates with some sprinkles of HTMX, all styled with Bootstrap 5